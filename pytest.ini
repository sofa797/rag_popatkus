[pytest]
minversion = 9.0

addopts = 
    -v
    --tb=short
    --cov=backend/app
    --cov=frontend
    --cov=shared
    --cov-report=term-missing
    --cov-report=xml

testpaths = 
    tests

pythonpath = . backend frontend shared
